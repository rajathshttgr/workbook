### Topics

- Process
- Thread
- Concurrency
- Parallelism
- CPU-bound vs I/O-bound tasks
- Context Switching

# 1. Python Threading

### Key Functions

- `Thread()` → creates a thread
- `start()` → starts execution
- `join()` → waits for thread to finish

# 2. Race Condition

When multiple threads modify the same variable, results may be incorrect.
