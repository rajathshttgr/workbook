### Topics

- Process
- Thread
- Concurrency
- Parallelism
- CPU-bound vs I/O-bound tasks
- Context Switching

# 1. Python Threading

### Key Functions

- `Thread()` → creates a thread
- `start()` → starts execution
- `join()` → waits for thread to finish

# 2. Race Condition

When multiple threads modify the same variable, results may be incorrect.

# 3. Lock to prevent Race conditions in thread shared memory

### Core Concepts

- Process
- Thread
- Concurrency
- Parallelism
- CPU-bound vs I/O-bound tasks
- Context Switching

### Python Modules & APIs

- `threading`
- `multiprocessing`
- `subprocess`
- `concurrent.futures`
- `asyncio`

### Multiprocessing Concepts

- Process creation
- Process Pool
- Inter-Process Communication (IPC)
- Shared memory
- Pipes
- Queues

### Advanced Topics

- Global Interpreter Lock (GIL)
- Futures
- Executors (ThreadPoolExecutor / ProcessPoolExecutor)
- Async tasks
- Coroutines
- Event loop
- Task scheduling
